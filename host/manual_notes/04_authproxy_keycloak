"ID Provider rejected client" --> make sure secrets yaml is correct vs keycloak.
    Authproxy needs those tokens to be correct but they're generated by keycloak (can't be pre-set) and not auto-updated
    (Could I auto-update? Sounds tough, probably involving some convoluted configmap chaining...)